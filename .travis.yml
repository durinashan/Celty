
sudo: false
language: cpp
compiler:
  - gcc
  - clang
addons:
   apt:
      sources:
         - kalakris-cmake
      packages:
         - cmake3
before_script:
  - mkdir build
  - cd build
  - cmake ../
script:
  - make
